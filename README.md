# realstate-app

To run the project, you need to install the dependencies and run the development server.

Navigate to the client directory and run the following commands:

```bash
npm install
npm run dev
```

Navigate to the server directory and run the following commands:

```bash
npm install
npm run dev
```
